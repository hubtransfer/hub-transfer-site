export const metadata = {
  title: "Motorista — HUB Transfer",
  description: "Painel do motorista — viagens do dia",
};

export default function DriverTripsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
