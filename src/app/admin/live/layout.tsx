export const metadata = {
  title: "LIVE — HUB Transfer OPS",
  description: "Acompanhamento em directo de todos os transfers",
};

export default function AdminLiveLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
