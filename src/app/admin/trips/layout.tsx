export const metadata = {
  title: "Viagens — HUB Transfer OPS",
  description: "Painel operacional de viagens",
};

export default function TripsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
